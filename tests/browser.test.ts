import { describe, test } from "vitest";

describe("browser lifecycle", () => {
  test.todo("launches browser on first capture");
  test.todo("reuses browser across captures");
  test.todo("closes browser context after capture");
  test.todo("closes browser on shutdown");
});

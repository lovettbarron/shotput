import { describe, test } from "vitest";

describe("MCP server", () => {
  test.todo("starts with stdio transport");
  test.todo("registers shotput_capture tool");
  test.todo("accepts url parameter");
});

import { describe, test } from "vitest";

describe("auto-scroll", () => {
  test.todo("triggers lazy images to load");
  test.todo("skips scroll when autoScroll is false");
  test.todo("resets scroll position to top after scrolling");
});
